import React, { FC } from "react";

import styled from "styled-components/native";

const ItemSeparatorComponent: FC = () => <Space />;

const Space = styled.View`
  height: 10px;
`;

export default ItemSeparatorComponent;
